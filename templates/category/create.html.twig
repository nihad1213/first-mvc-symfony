{% extends 'base.html.twig' %}

{% block title %}Create Category{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>Create Category</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}

        <button class="btn btn-success mt-3">
            Save
        </button>
        <a href="{{ path('app_category_index') }}" class="btn btn-secondary mt-3">
            Back
        </a>
    {{ form_end(form) }}
</div>
{% endblock %}
